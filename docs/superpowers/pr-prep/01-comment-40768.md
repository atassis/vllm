<!--
  НАЗНАЧЕНИЕ: комментарий в PR #40768 (@z1ying).
  Тон: признать их работу, не топтать, предложить строить поверх + дать тесты.
  Запостить ДО открытия своего PR. Текст ниже — на английский, как есть.
-->

Hi @z1ying — thanks for this fix. I've been working on **MTP + pipeline-parallel (PP > 1)
speculative decoding** (closing #36643 / #36872), and your PR addresses exactly the
scheduler-side half of what I hit: stale async `-1` placeholders leaking into the
embedding lookup when a request wasn't in the previous worker batch.

Full transparency: to unblock my own testing I ported your scheduler change onto current
`main`, so my version of this piece is **effectively identical to yours** (same
`num_pending_async_spec_placeholders` intent + `_consume_spec_decode_tokens_for_step`
gated on `prev_step_scheduled_req_ids`). I do **not** want to open a competing/duplicate
scheduler PR — I'd rather drop mine, depend on this one, and help land it.

Three things I can contribute here if useful:

1. **End-to-end validation under MTP + PP > 1.** Your repro is single-GPU (GLM-OCR +
   multimodal); I've confirmed the same placeholder discipline is necessary on the
   multi-stage path — on the non-last PP rank the worker-side overwrite is skipped for the
   same reason, so this fix is load-bearing there too.
2. **A heads-up for when you rebase onto current `main`:** `_update_after_schedule` is now
   called at the *end* of `schedule()` (around `scheduler.py:943`), which re-reserves the
   placeholder intent for the next step right after `_consume_spec_decode_tokens_for_step`
   clears it (in your base it was called at schedule-start). Worth a look so the intent
   isn't double-counted after a rebase.
3. **Additional test coverage** for the re-added-after-preemption and
   newly-scheduled-this-step paths (CPU-only). Happy to push as a suggestion here or keep
   in my follow-up — your call.

To be transparent: my downstream work fixes the *correctness* layer that sits on top of
this (non-last-rank token/position/accepted-count accounting so MTP + PP is
greedy-equivalent), and it **depends on this placeholder discipline** being in place. I'll
reference this PR as a dependency and won't duplicate the scheduler changes.

Would you prefer I (a) push my tests as a suggestion/commit here, or (b) keep them in my
follow-up PR and just link back? Either works — mainly want to avoid stepping on your work
while making sure the PP + MTP path is covered.

> Note: AI assistance was used in diagnosing and drafting this.
