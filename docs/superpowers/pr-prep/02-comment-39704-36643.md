<!--
  ДВА коротких комментария. Цель: вежливо застолбить заявку и предупредить дубли.
  A) в PR #39704 (@knight0528).  B) в issue #36643 (тред на 11 комментов).
-->

=====================================================================
A) Комментарий в PR #39704 («Feat: add support for PP and MTP», @knight0528)
=====================================================================

Hi @knight0528 — are you still actively working on this? I've been independently
implementing MTP + PP > 1 speculative decoding (targeting #36643 / #36872) and have a
working, **greedy-equivalence-verified** implementation, so I want to coordinate rather
than duplicate.

The main differences in my approach, in case they're useful to you or to reviewers
comparing the two:

- **Verified greedy-equivalence** against the no-spec baseline (token-identical, MiMo
  PP=2: 40 tokens 5/5 == baseline, deterministic across runs), not just "it runs".
- **Hybrid (GDN/mamba) models handled** — the non-last rank also rolls back the conv1d/SSM
  recurrent state via the broadcast accepted-count, which is required for Qwen3.5-27B.
- **Memory enablement** so the draft (embed + lm_head) fits on the last PP rank for large
  models (int4 draft-embed load-time quant + skipping the draft lm_head allocation).
- **Unit + integration tests** (CPU gloo transport round-trip, drift-correction units,
  scheduler discipline) plus an e2e speedup benchmark.

Happy to coordinate — if this PR is stalled I'll open mine referencing it and explaining
the delta; if you're still on it, let's compare notes so we don't both burn review cycles.

> AI assistance was used in this work.


=====================================================================
B) Комментарий в issue #36643 («Qwen3.5 does not work with pipeline parallelism»)
=====================================================================

Heads-up for this thread: I have an MTP + PP > 1 fix in preparation that makes Qwen3.5
(both the pure-attention MiMo-style and the hybrid GDN 27B) run **greedy-equivalent** to
the single-GPU / no-spec baseline under PP=2, with a measured speedup. It builds on the
async-placeholder scheduler fix in #40768.

PR incoming shortly; linking here so anyone hitting this isn't blocked silently and to
avoid duplicate effort. If you have a specific Qwen3.5 + PP config that reproduces for you,
drop it here and I'll make sure it's covered.

> AI assistance was used in diagnosing and fixing this.
