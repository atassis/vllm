# PR-prep — тексты для апстрима (черновики)

> Папка untracked (внутри `docs/superpowers/`). Наружу не уходит. Это **черновики**,
> которые ты, как human submitter, обязан вычитать и подтвердить построчно (AGENTS.md:
> чисто-агентские PR запрещены, ты защищаешь каждую строку и сам гоняешь тесты).

## Порядок действий (этикет сначала, код потом)

1. **`01-comment-40768.md`** → запостить комментарием в PR #40768 (@z1ying).
   Самый чувствительный к этикету шаг: признаём чужую заявку на scheduler-фикс (наш C3),
   предлагаем строить PP-корректность ПОВЕРХ него + дать тесты. Превращает overlap в
   коллаборацию. **Сделать ДО открытия своего PR.**

2. **`02-comment-39704-36643.md`** → два коротких комментария:
   - в PR #39704 (@knight0528, «add support for PP and MTP») — вежливо застолбить заявку,
     спросить, активны ли они / координируемся;
   - в issue #36643 («Qwen3.5 does not work with PP», 11 комментов) — сказать, что фикс
     на подходе, чтобы люди в треде знали.

3. **`03-pr-description.md`** → описание ОДНОГО feature-PR (A1c + B1a + C4 + s9), **без C3**.
   Покоммитные слои внутри одного PR, `Closes #36643/#36872`, `depends on #40768`,
   явная разница vs #39704/#38104, test plan + результаты + заявление об AI-ассистенции.

## Что вырезано и почему

- **C3 (scheduler placeholder discipline)** НЕ входит в PR — это дубликат живого #40768
  (те же 5 файлов, тот же `-1`-leak). Уходит в координацию (шаг 1). Это обязательно по
  этикету и по AGENTS.md, не по вкусу.

## ⚠️ Что ТЫ должен подтвердить перед постингом (плейсхолдеры в текстах помечены `‹…›`)

- точные tok/s и тип GPU в результатах бенча (у меня из KB: MiMo 39.66 vs 22.70 = 1.75×,
  27B-AWQ 1.82×, но впиши то, что реально в последнем прогоне);
- на какой коммит main / поверх какого состояния #40768 перебазировано;
- что `pre-commit run --all-files` и целевые pytest реально зелёные ПОСЛЕ финального ребейза;
- номера issue, которые реально закрываются (проверь, что #36643/#36872 ещё открыты и про то).

## Источник фактов

Технические утверждения — из KB (`docs/superpowers/state.yml`, brick-40/81). Коммиты:
`8105121a9` (s9 num_computed_tokens drift), `bd3ad37b8` (s9 hybrid num_accepted_tokens),
A1c в `e45e5d462`. Сверяй с `git show` перед публикацией.
